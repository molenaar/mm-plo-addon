---
name: mm-plo-orchestrator
classification: simple-workflow
customizable: true
supports_headless: true
workspace_layout: folder
last_touched: 2026-06-13
---

# Decision Log

## 2026-06-13 — Discovery

- Named the workflow `mm-plo-orchestrator` to preserve the user's `mm-*` addon convention.
- Chose a simple inline workflow with named stages; no carved-out reference files are needed yet.
- Enabled customization so tracker, prompt catalog, lane map, and output workspace paths can be overridden in other repos.
- Adjusted the defaults to the repo's canonical artifact locations: sprint tracker under `implementation-artifacts`, prompt catalog and lane map under `docs/operations/`.
- Switched the prompt catalog default to the `docs/operations` directory and kept the lane map as a concrete YAML file path beneath it.
- Kept the scope orchestration-only and treated `bmad-sprint-status`, `bmad-agent-dev`, `bmad-code-review`, and `bmad-qa-generate-e2e-tests` as authoritative dependencies.
- Chose workspace-folder outputs: round summary, lane bundles, dispatch block, closure notes, and tracker state.
- Supported both interactive and `--headless` runs.
- Packaged the skill as a standalone module with module code `mplo` and validated it at the module root (`skills/`).
